// Author: Jacopo Daeli <jacopo.daeli@gmail.com>// SelectionSort Algorithmvar utilArray = require('../util/array');var utilPrimitive = require('../util/primitive');// Sort A[] into increasing orderfunction selection_sort(A) {  var N = A.length; // array length  // Exchange A[i] with smallest entry in A[i+1...N)  for (var i = 0; i < N-1; i++) {    var min = i; // index of a minimal entry    for (var j = i+1; j < N; j++) {      if (utilPrimitive.less(A[j], A[min])) {        min = j;      }    }    utilArray.swap(A, i, min);  }}module.exports = selection_sort;